spring:
  application:
    name: tmall
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
        refresh-enabled: true
        group: DEFAULT_GROUP
      discovery:
        server-addr: localhost:8848


